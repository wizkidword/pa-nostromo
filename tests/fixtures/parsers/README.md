# Parser fixtures

These are small, sanitized samples used to detect parser drift. They contain no
credentials, account identifiers, real feed content, or production URLs. Add a
fixture only when it demonstrates a stable required structure or a known failure
mode; do not save full upstream pages or imported user data here.
